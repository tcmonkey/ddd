# 决策记录

| ID | 日期 | 决策 | 原因 | 备选方案 | 影响范围 | 决策人 |
|---|---|---|---|---|---|---|
| DEC-001 | 2026-09-14 | `ddd` 同时作为模板验收项目和 Java DDD 参考工程。 | 用户希望先真实验证模板，再沉淀可复用代码框架。 | 只评审文档或只生成代码。 | 全项目 | 用户 |
| DEC-002 | 2026-09-14 | 人类阅读文档必须为 Markdown，机器状态允许 JSON/YAML。 | 兼顾可读性和确定性状态处理。 | 全部文件强制 Markdown。 | 文档与状态协议 | 用户 |
| DEC-003 | 2026-09-14 | 当前无 UI 需求，跳过 UI 阶段。 | 避免生成无实际价值的 UI 文档。 | 保留空 UI 阶段。 | 工作流 | 用户 |
| DEC-004 | 2026-09-14 | 使用 `AI/input` 存放输入素材，`AI/output` 存放生成文档，`.ai-delivery` 仅保存机器状态和配置。 | 用户要求输入输出清晰分离；Skill 0.5 已原生支持该结构。 | 全部保留在 `.ai-delivery`、复制两份或使用符号链接。 | 目录与跨宿主连续性 | 用户/Agent |
| DEC-005 | 2026-09-14 | 生成文档模板和阶段规范统一使用中文文件名；`SKILL.md` 等协议入口保留标准名称。 | 提高中文项目的可读性，同时保持宿主发现和打包兼容。 | 全部保留英文名或连协议入口一起改名。 | 项目文档与公共 Skill | 用户/Agent |
| DEC-006 | 2026-09-14 | 生成的输入、输出、交付记录与 Skill 模板采用固定两位序号加中文名称；`00` 是输入材料，`01`～`13` 是交付阶段，`14`～`18` 是全程记录。 | 让 Finder、IDE 和不同宿主默认按交付顺序显示文件。 | 只依赖人工排序、无序号中文名或为每个目录使用不同规则。 | 项目文档、公共 Skill、初始化与迁移脚本 | 用户/Agent |
| DEC-007 | 2026-09-14 | 技术方案先采用“背景与价值 → 调研与矛盾 → 整体方案 → 事项边界 → 人天评估”的聚合式项目草案；伪代码是核心必填内容。 | 让开发者通过一份文档理解完整编码闭环，避免零散专题淹没核心逻辑。 | 继续使用十余个并列技术专题标题。 | 技术方案模板与后续开发计划 | 用户/Agent |
| DEC-008 | 2026-09-14 | 以“会员积分发放”作为 Java DDD 参考工程的默认垂直切片，覆盖写、读、规则计算和纯计算。 | 在未提供真实业务时，选择能完整展示 DDD 调用链且易替换的最小领域。 | 仅生成空接口、选择复杂生产业务或等待业务主题后再开始。 | 技术方案与后续代码 | Agent，待用户确认 |
| DEC-009 | 2026-09-14 | 采用 JDK 17、Spring Boot 3.x、Maven、JUnit 5，并确认会员积分发放作为首版示例。 | 用户已确认技术基线和样例业务。 | 延迟至真实项目再建工程。 | 工程初始化、依赖、测试与示例领域 | 用户 |
| DEC-010 | 2026-09-14 | 开发计划生成后直接进入首版编码，不设置单独的计划评审阻塞。 | 用户希望先阅读可修改的代码，并将其作为未来模板样例。 | 先等待计划确认再编码。 | 本轮开发流程 | 用户 |
| DEC-011 | 2026-09-14 | `client`、`model`、`domain`、`application`、`infrastructure`、`adaptor`、`start` 均为 Maven 子模块；根项目仅聚合与管理版本。 | 用户澄清 DDD 分层目录是模块边界，而非单模块 package。 | 单 Maven module 下按 package 分层。 | 构建结构、依赖方向、测试归属 | 用户 |
| DEC-012 | 2026-09-15 | 参考工程、目录和根 Maven 坐标统一命名为 `ddd`；全部子模块统一使用 `ddd-` 前缀，Java 根包为 `com.ddd`。 | 使参考工程更像可复制的 DDD 模板，避免旧项目标识和泛化演示名称残留。 | 保留旧目录与坐标，或仅改显示名称。 | 目录、构建、源码、测试、文档与状态 | 用户 |
| DEC-013 | 2026-09-15 | 启动类使用 `Application`，外层响应使用 `Result`；aggregate/entity/param/value 使用明确后缀（entity 采用标准拼写 `Entity`）。application 使用 Spring `@Service` 构造器注入，domain 以无框架 `@DomainService` 标记由 start 扫描；MyBatis-Plus Mapper 由 `@MapperScan` 集中注册。根 POM 使用 `spring-boot-starter-parent`。 | 落实用户提出的命名、注入与 MyBatis-Plus 参考规范，同时保持 domain 不依赖 Spring。 | 在 domain 引入 Spring、逐个 Mapper 使用 `@Mapper`、或继续只使用内存仓储。 | 启动、协议、领域模型、依赖、持久化与测试 | 用户/Agent |
| DEC-014 | 2026-09-15 | 所有源码注释使用中文 Javadoc；账户领域仓储查询直接返回聚合根，不返回 `Optional`，保存返回 `Boolean`。MyBatis-Plus 仓储继承 `DddBaseRepository → CrudRepository` 复用 CRUD，Mapper 禁止自定义 SQL。 | 形成清晰的领域仓储契约并减少基础 CRUD 样板代码。 | 仓储返回 `Optional`、手写 Mapper SQL 或在业务仓储中重复基础 CRUD。 | 领域端口、基础设施仓储、应用编排、源码注释与测试 | 用户 |
| DEC-015 | 2026-09-15 | 四种 DDD 模式均保留独立可调用样例；外部读模式由 application 定义端口，adaptor output 以 converter 将模拟第三方响应转换为 `ddd-model` 内部模型。 | 让参考工程展示完整包结构，并使真实外部调用可只替换 output 实现。 | 只保留 input adaptor、将第三方 DTO 传入 application/domain、或在 application 直接调用第三方。 | client、model、application、adaptor 与接口测试 | 用户 |
| DEC-016 | 2026-09-15 | 删除具体业务案例，业务类型和字段统一改为 `Ddd*` 中性占位符；未来项目按领域语言替换前缀并重新建模。Spring Boot 启动类仍为 `Application`。 | 公共代码模板应表达职责和调用模式，不应诱导后续项目复制虚构业务；同时遵守已确认的启动类命名。 | 保留案例、使用空接口，或把启动类也改成 `DddApplication`。 | 产品方案、技术方案、开发计划、源码、测试与数据表 | 用户/Agent |
| DEC-017 | 2026-09-15 | Java 源码注释一律采用多行中文 Javadoc，每个注释都带 `@author AIGenerator`；真实项目可按团队要求替换作者。 | 统一公共模板的注释形态并保留作者信息，不把某位成员写死在通用示例中。 | 保留单行 Javadoc、只在类级注释加作者或写入个人姓名。 | Java 源码、测试、技术方案和开发计划 | 用户/Agent |
| DEC-018 | 2026-09-15 | 工程、内部模块和插件依赖版本集中在根 POM；子模块不声明 dependency/plugin 版本，唯独 parent 版本按 Maven 模型要求保留。POM dependency 与 Java import 均按可读的多行和分组格式编排。 | 消除各 module 的版本重复和单行依赖声明，同时保持 Maven 父子关系可解析。 | 子模块保留 `${project.version}`、使用压缩单行 dependency，或尝试省略必需 parent 版本。 | 根与 7 个模块 POM、Java import 和技术方案 | 用户/Agent |
| DEC-019 | 2026-09-15 | PO 和领域聚合的状态字段必须说明用途、表列映射或不变量；record 组件在类 Javadoc 中逐项以 `@param` 解释；PO getter/setter 使用多行常规格式。 | 未来开发者可以直接从数据类理解字段和边界，不必先追转换代码。 | 只写类级泛化说明、把访问器压在单行或给每个简单访问器重复注释。 | 数据模型、源码格式、技术方案与开发自测 | 用户/Agent |
| DEC-020 | 2026-09-15 | Spring 管理的组件依赖统一显式标注 `@Autowired`：Controller、application、output adaptor 优先字段注入；MyBatis-Plus 仓储因必须初始化基类 Mapper 使用显式 `@Autowired` 构造器。domain 不引入 Spring，沿用 start 装配。此决策覆盖 DEC-013 中 application 构造器注入的旧约定。 | 用户明确选择统一注入标记及更简洁的普通组件写法，同时保留 Maven/DDD 模块边界和仓储基类能力。 | 继续使用隐式构造器注入、改用 `@Resource`，或为注入注解让 domain 依赖 Spring。 | Controller、application、output adaptor、infrastructure 仓储、技术方案与自测 | 用户/Agent |
| DEC-021 | 2026-09-15 | Controller 每个 HTTP 入口方法均保留用途、参数、结果和作者齐全的中文 Javadoc；HTTP Controller 不为协议暴露专门写接口。若新增 RPC，供外部依赖的接口在 client 二方包中声明并详细记录方法契约，由 adaptor/input 实现。 | 用户选择统一可读的方法注释，并区分 HTTP 映射与需要二方包的 RPC 契约。 | 删除 Controller 方法注释、当前先建无调用方的空 RPC 接口或把 RPC 实现放入 client。 | input adaptor、client、技术方案与开发自测 | 用户/Agent |
| DEC-022 | 2026-09-15 | Java 方法和构造器签名优先写在一行，仅完整签名超过 120 字符截止线时换行；record 组件声明按字段列表排版另行判断。 | 用户指出 Controller 中提前换行造成无意义的视觉割裂；沿用工程已有行宽阈值。 | 一律按参数个数换行、保留不足截止线的多行签名或强行压缩 record 字段列表。 | Java 源码、技术方案与排版自测 | 用户/Agent |
| DEC-023 | 2026-09-15 | 参考工程持久化样例改为唯一的 `ddd_data` 表：领域实体仍由聚合持有，其完整操作记录保存为同一行的 `entities_json`。`DddBaseRepository` 仅继承 MyBatis-Plus `CrudRepository`，Mapper 复用父类已有 `@Autowired`，移除第二套 PO/Mapper/Repository。此决策纠正 DEC-020 中“基类必须由构造器传 Mapper”的误判。 | 用户确认执行单表收敛；itineary 同类仓储及 MyBatis-Plus 3.5.17 的基类行为证明额外构造器不必要。 | 保留双表样例、删掉领域实体和幂等、或把 Mapper 注入写两遍。 | 基础设施、schema、集成测试、技术方案和开发自测 | 用户/Agent |
| DEC-024 | 2026-09-15 | 删除 `InMemoryDddRepository` 和 `InMemoryDddRuleRepository`；主聚合仍以单张 `ddd_data` 表保存实体快照，规则聚合另以独立空表 `ddd_rule` 和 `DddRuleRepositoryImpl` 展示正式 MyBatis-Plus 仓储链路。测试规则只在集成测试中插入，运行期不预置。此决策将 DEC-023 的“唯一表”限定为主聚合样例，而非全工程。 | 用户指出内存规则仓储不符合公共代码规范模板，要求仓储结构明确且没有运行期演示数据。 | 保留硬编码内存规则、返回虚构聚合或让规则链路缺失仓储实现。 | infrastructure、schema、启动装配、测试、技术方案及开发记录 | 用户/Agent |
| DEC-025 | 2026-09-15 | 删除只注册时钟和输入组装器的 `DddConfiguration`；无状态 `DddInputAssembler` 直接使用 `@Component`，写入时间直接使用 `Instant.now()`。 | 当前没有自定义时钟、固定时间测试或条件装配；两个对象均不需要手动 Bean 工厂。 | 保留手动 Bean、给应用服务注入 `Clock`，或在 Controller 中每次自行创建组装器。 | adaptor/input、application/write、start 装配、技术方案与开发自测 | 用户/Agent |
| DEC-026 | 2026-09-15 | 在启动模块增加 `logback-spring.xml`：默认仅控制台，`prod` 同时启用按日期与大小滚动的文件；目录和根级别由环境变量覆盖，本地 `logs/` 不入库。MDC `traceId` 只预留输出槽位，未宣称已经实现请求追踪。 | 用户指出日志配置缺失；公共模板需要可运行的默认值，也需避免测试写文件及硬编码部署目录。 | 无配置仅依赖框架默认日志、所有 profile 都写文件、或在公共模板绑定固定生产日志路径。 | start 资源、Git 忽略、技术方案和开发自测 | 用户/Agent |
| DEC-027 | 2026-09-15 | 运行组件统一使用单一构造器注入；`@DomainService` 仍是无 Spring 依赖的扫描标记，领域服务以普通构造器接收领域仓储端口。写模式中 `DddAggregate` 只持有 `DddEntity` 根实体；根实体内聚 id、当前值、版本和 `DddOperationEntity` 子实体。Application 只传 `DddWriteParam(aggregate)`，DomainService 完成加载、规则处理、实体确认和仓储保存。 | 用户希望消除字段注入及聚合根重复状态，同时由领域服务完成仓储写入。单一构造器既保持 domain 源码无 Spring API，又让依赖显式。 | 继续字段注入、在 domain 引入 `@Autowired`、让可变 Aggregate 作为 Spring Bean、或将未初始化字段伪装为有效实体。 | domain、application、adaptor、infrastructure、测试及设计文档 | 用户/Agent |
| DEC-028 | 2026-09-15 | 写 Application 不直接构造或读取 `DddIdValue`、`DddOperationIdValue`、`DddValue`、`DddOperationEntity`。它只传递 Command 原始字段给 `DddAggregate.draft`，后者调用根 `DddEntity` 完成值对象和输入子实体封装。 | 值对象校验和实体构造是领域模型规则，不应因应用层组装而泄漏。 | Application 手动 new Value Object/Entity，或将领域值对象直接暴露为应用命令字段。 | 写 application、聚合根、技术方案、自测与未来代码生成规则 | 用户 |
| DEC-029 | 2026-09-15 | 新增 `ddd-common`，以同一个 `Result<T>`、`ErrorCode`、`BaseException` 覆盖全部分层。`DomainErrorCode`、`ApplicationErrorCode`、`InfrastructureErrorCode`、`AdaptorErrorCode` 分别由对应模块枚举实现。DomainService 和 OutAdaptor 的公开调用入口捕获异常、记录一次日志并返回 Result；私有方法、Entity、Aggregate 只抛异常。Application 保留 command/result，始终将领域决策或 `DddModel` 转为 Application Result 后才交给 Controller。 | 用户需要上层统一判断成功、内部错误码和友好文案，同时要求日志保留根因且不让客户端、Controller 或 domain 被外部协议污染。 | 在 `ddd-client` 放公共 Result、下游原始错误码直接透传、每层重复 catch/log，或让 Controller 直接处理 `DddModel`。 | 所有模块、主调用链、错误日志、HTTP 协议与代码生成模板 | 用户 |
