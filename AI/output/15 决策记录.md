# 决策记录

| ID | 日期 | 决策 | 原因 | 备选方案 | 影响范围 | 决策人 |
|---|---|---|---|---|---|---|
| DEC-001 | 2026-09-14 | `ddd` 同时作为模板验收项目和 Java DDD 参考工程。 | 用户希望先真实验证模板，再沉淀可复用代码框架。 | 只评审文档或只生成代码。 | 全项目 | 用户 |
| DEC-002 | 2026-09-14 | 人类阅读文档必须为 Markdown，机器状态允许 JSON/YAML。 | 兼顾可读性和确定性状态处理。 | 全部文件强制 Markdown。 | 文档与状态协议 | 用户 |
| DEC-003 | 2026-09-14 | 当前无 UI 需求，跳过 UI 阶段。 | 避免生成无实际价值的 UI 文档。 | 保留空 UI 阶段。 | 工作流 | 用户 |
| DEC-004 | 2026-09-14 | 使用 `AI/input` 存放输入素材，`AI/output` 存放生成文档，`.ai-delivery` 仅保存机器状态和配置。 | 用户要求输入输出清晰分离；Skill 0.5 已原生支持该结构。 | 全部保留在 `.ai-delivery`、复制两份或使用符号链接。 | 目录与跨宿主连续性 | 用户/Agent |
| DEC-005 | 2026-09-14 | 生成文档模板和阶段规范统一使用中文文件名；`SKILL.md` 等协议入口保留标准名称。 | 提高中文项目的可读性，同时保持宿主发现和打包兼容。 | 全部保留英文名或连协议入口一起改名。 | 项目文档与公共 Skill | 用户/Agent |
| DEC-006 | 2026-09-14 | 生成的输入、输出、交付记录与 Skill 模板采用固定两位序号加中文名称；`00` 是输入材料，`01`～`13` 是交付阶段，`14`～`18` 是全程记录。 | 让 Finder、IDE 和不同宿主默认按交付顺序显示文件。 | 只依赖人工排序、无序号中文名或为每个目录使用不同规则。 | 项目文档、公共 Skill、初始化与迁移脚本 | 用户/Agent |
| DEC-007 | 2026-09-14 | 技术方案先采用“背景与价值 → 调研与矛盾 → 整体方案 → 事项边界 → 人天评估”的聚合式项目草案；伪代码是核心必填内容。 | 让开发者通过一份文档理解完整编码闭环，避免零散专题淹没核心逻辑。 | 继续使用十余个并列技术专题标题。 | 技术方案模板与后续开发计划 | 用户/Agent |
| DEC-008 | 2026-09-14 | 以“会员积分发放”作为 Java DDD 参考工程的默认垂直切片，覆盖写、读、规则计算和纯计算。 | 在未提供真实业务时，选择能完整展示 DDD 调用链且易替换的最小领域。 | 仅生成空接口、选择复杂生产业务或等待业务主题后再开始。 | 技术方案与后续代码 | Agent，待用户确认 |
| DEC-009 | 2026-09-14 | 采用 JDK 17、Spring Boot 3.x、Maven、JUnit 5，并确认会员积分发放作为首版示例。 | 用户已确认技术基线和样例业务。 | 延迟至真实项目再建工程。 | 工程初始化、依赖、测试与示例领域 | 用户 |
| DEC-010 | 2026-09-14 | 开发计划生成后直接进入首版编码，不设置单独的计划评审阻塞。 | 用户希望先阅读可修改的代码，并将其作为未来模板样例。 | 先等待计划确认再编码。 | 本轮开发流程 | 用户 |
| DEC-011 | 2026-09-14 | `client`、`model`、`domain`、`application`、`infrastructure`、`adaptor`、`start` 均为 Maven 子模块；根项目仅聚合与管理版本。 | 用户澄清 DDD 分层目录是模块边界，而非单模块 package。 | 单 Maven module 下按 package 分层。 | 构建结构、依赖方向、测试归属 | 用户 |
| DEC-012 | 2026-09-15 | 参考工程、目录和根 Maven 坐标统一命名为 `ddd`；全部子模块统一使用 `ddd-` 前缀，Java 根包为 `com.ddd`。 | 使参考工程更像可复制的 DDD 模板，避免旧项目标识和泛化演示名称残留。 | 保留旧目录与坐标，或仅改显示名称。 | 目录、构建、源码、测试、文档与状态 | 用户 |
| DEC-013 | 2026-09-15 | 启动类使用 `Application`，外层响应使用 `Result`；aggregate/entity/param/value 使用明确后缀（entity 采用标准拼写 `Entity`）。application 使用 Spring `@Service` 构造器注入，domain 以无框架 `@DomainService` 标记由 start 扫描；MyBatis-Plus Mapper 由 `@MapperScan` 集中注册。根 POM 使用 `spring-boot-starter-parent`。 | 落实用户提出的命名、注入与 MyBatis-Plus 参考规范，同时保持 domain 不依赖 Spring。 | 在 domain 引入 Spring、逐个 Mapper 使用 `@Mapper`、或继续只使用内存仓储。 | 启动、协议、领域模型、依赖、持久化与测试 | 用户/Agent |
| DEC-014 | 2026-09-15 | 所有源码注释使用中文 Javadoc；账户领域仓储查询直接返回聚合根，不返回 `Optional`，保存返回 `Boolean`。MyBatis-Plus 仓储继承 `DddBaseRepository → CrudRepository` 复用 CRUD，Mapper 禁止自定义 SQL。 | 形成清晰的领域仓储契约并减少基础 CRUD 样板代码。 | 仓储返回 `Optional`、手写 Mapper SQL 或在业务仓储中重复基础 CRUD。 | 领域端口、基础设施仓储、应用编排、源码注释与测试 | 用户 |
| DEC-015 | 2026-09-15 | 四种 DDD 模式均保留独立可调用样例；外部读模式由 application 定义端口，adaptor output 以 converter 将模拟第三方响应转换为 `ddd-model` 内部模型。 | 让参考工程展示完整包结构，并使真实外部调用可只替换 output 实现。 | 只保留 input adaptor、将第三方 DTO 传入 application/domain、或在 application 直接调用第三方。 | client、model、application、adaptor 与接口测试 | 用户 |
| DEC-016 | 2026-09-15 | 删除具体业务案例，业务类型和字段统一改为 `Ddd*` 中性占位符；未来项目按领域语言替换前缀并重新建模。Spring Boot 启动类仍为 `Application`。 | 公共代码模板应表达职责和调用模式，不应诱导后续项目复制虚构业务；同时遵守已确认的启动类命名。 | 保留案例、使用空接口，或把启动类也改成 `DddApplication`。 | 产品方案、技术方案、开发计划、源码、测试与数据表 | 用户/Agent |
